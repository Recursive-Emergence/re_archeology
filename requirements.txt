# Core FastAPI and server dependencies
fastapi==0.104.1
uvicorn[standard]==0.23.2  # includes uvloop and httptools for better performance

# Neo4j driver with optimizations
neo4j==5.15.0

# OpenAI and AI functionality
openai==1.12.0

# SEMANTIC SEARCH DEPENDENCIES - UNCOMMENT WHEN IMPLEMENTING SEMANTIC SEARCH
# These packages pull in large dependencies (including PyTorch) that take up significant disk space
# torch==2.1.0+cpu --extra-index-url https://download.pytorch.org/whl/cpu
# sentence-transformers==2.2.2

# Google Auth
google-auth==2.23.0
google-auth-oauthlib==1.0.0
google-auth-httplib2==0.1.1
PyJWT==2.8.0

# WebSocket and real-time features with performance optimizations
websockets==12.0
redis==5.0.1
aioredis==2.0.1  # Async Redis client for better performance

# Scientific computing and geospatial libraries
geopandas==0.14.0
numpy==1.26.0
scipy==1.11.3
pandas==2.1.1
earthengine-api==0.1.370
google-cloud-storage==2.10.0

# Validation and configuration
pydantic==2.4.2
pydantic-settings==2.0.3
python-dotenv==1.0.0

# Testing and development
pytest==7.4.2
httpx==0.25.0

# Additional geospatial tools
shapely==2.0.2
rasterio==1.3.8
geojson==3.1.0

# Required for form data handling
python-multipart==0.0.6
tabulate==0.9.0

# Performance monitoring and optimization
psutil==5.9.6  # System monitoring
orjson==3.9.10  # Faster JSON serialization
aiofiles==23.2.1  # Async file operations

# Production optimizations
gunicorn==21.2.0  # Production WSGI server alternative
